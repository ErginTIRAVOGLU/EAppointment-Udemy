### MCP Server Config
Added a sample configuration for connecting to the MCP server.

```json
{
	"servers": {
		"appointment-mcp-server": {
			"url": "http://localhost:5001/mcp",
			"type": "http"
		}
	},
	"inputs": []
}
```